import { connect } from 'react-redux';
import NewTeam from './new_team';

const msp = state => {
  return {

  };
};

const mdp = dispatch => {
  return {

  };
};

export default connect(msp, mdp)(NewTeam);
